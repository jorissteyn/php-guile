// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("guile", "for guile support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("guile", "enable guile support", "no");

if (PHP_GUILE != "no") {
	EXTENSION("guile", "guile.c");
}

